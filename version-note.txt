0.35.71: Correct the R100 performance mistake. Remove the adaptive render-resolution downshift so the game no longer lowers internal render quality/rate under pressure. Restore the forward world buffer to one aisle behind and three finished aisles ahead with four boot aisles so the horizon feels like store space again instead of a hard fog/cutoff. Keep the tighter 4x4 occupied-cell compound collision from R100, but prevent runtime BatchedMesh construction while gameplay is active and disable per-object batch frustum churn on existing engine batches to reduce camera-turn spikes. Distance haze is pushed farther out and warmed to read as ambient store air instead of a fake wall. Background work still waits for real idle time, but it is no longer over-throttled.
