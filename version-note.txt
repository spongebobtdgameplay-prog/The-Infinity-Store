0.35.69: Fix the third-person wall clipping radius mismatch (the 0.48m player was being clamped to 0.32m), move missing-object collision into the Three.js engine core using cheap compound oriented mesh bounds, and remove the legacy per-model triangle-footprint collision generator. Remove the distance-haze Renderer.render monkey-patch that forced every active aisle visible and defeated frustum streaming. Keep only one aisle behind and three ahead, boot four aisles, and retain only one back aisle so old unseen furniture is released instead of accumulating. Fog/camera distance now hide the streaming boundary while game.js remains the single render-visibility authority.
